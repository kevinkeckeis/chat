import React from 'react';

const ProjectScope = () => {
  return (
    <div className='grid h-full place-items-center'>
      Not part of the project scope yet.
    </div>
  );
};

export default ProjectScope;
